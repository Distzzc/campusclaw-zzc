## 1. Flask 应用与运行基础

- [ ] 1.1 建立 Flask 应用、依赖清单、测试目录和环境变量配置模块；verify：运行项目测试/导入检查，确认 Flask 应用可启动
- [ ] 1.2 创建 Docker Compose 编排、SQLite 与材料目录持久化卷、非生产环境变量示例和启动文档；verify：执行 `docker compose config` 成功
- [ ] 1.3 实现不需要认证的 `GET /health` liveness 端点；verify：启动 Flask 应用后用 HTTP 请求确认返回成功 JSON 且不泄露密钥

## 2. 数据模型、迁移与预置数据

- [ ] 2.1 建立 SQLite 的用户、角色、班级归属、材料和索引状态模型及幂等迁移；verify：运行迁移测试，确认教师/学生和 A/B 两班可区分
- [ ] 2.2 增加仅开发/测试启用的预置数据加载命令，提供每班教师和学生及非明文密码哈希；verify：重复执行命令不产生重复数据，生产配置不会自动加载预置数据
- [ ] 2.3 接入服务端环境变量配置校验，确保会话密钥、数据库路径和存储配置不从请求读取；verify：缺失必需密钥时配置测试快速失败，伪造请求密钥不会改变服务端配置
- [ ] 2.4 实现 Argon2id 或经审查的等价自适应密码哈希及安全比较；verify：持久化测试确认数据库只保存哈希，响应和日志不包含明文密码

## 3. 会话、登录与服务端授权

- [ ] 3.1 实现教师/学生登录、登出和 Flask session 或等价短期令牌；verify：接口测试确认有效登录包含用户、角色、班级，错误凭据不建立会话
- [ ] 3.2 实现受保护页面的未登录重定向和受保护 API 的 401；verify：浏览器/接口测试分别确认重定向到登录页和无保护数据的 401 响应
- [ ] 3.3 实现服务端角色授权策略；verify：学生直接调用上传 API 返回 403，且没有材料记录、原始文件或索引产生
- [ ] 3.4 实现从认证上下文取得 `class_id` 的服务端过滤；verify：伪造其他班级参数不会改变查询范围，A/B 班跨班请求返回 403 且不泄露元数据

## 4. 材料上传与知识库入库

- [ ] 4.1 实现原始材料存储和知识库索引适配器，以及待处理/成功/失败状态迁移；verify：集成测试确认成功记录同时具有文件引用和知识库索引状态
- [ ] 4.2 实现教师上传接口，按“认证/班级范围 → 校验 → SQLite 待处理记录 → 文件存储 → 知识库写入 → 成功状态”执行；verify：教师上传接口返回成功并关联真实班级
- [ ] 4.3 实现存储或索引失败的清理/补偿路径；verify：故障注入测试确认失败响应不会让成功材料出现在默认列表
- [ ] 4.4 实现本班材料列表和详情读取；verify：教师上传后同班教师/学生列表可查到记录，跨班读取和列表均被拒绝

## 5. 前端访问流程

- [ ] 5.1 增加登录页、认证状态恢复和受保护路由守卫；verify：端到端测试确认未登录访问跳转登录，登录后可进入受保护页面
- [ ] 5.2 根据服务端角色状态展示教师上传流程，为学生隐藏入口但保留服务端 403；verify：端到端测试确认教师可以上传、学生直接调用仍被拒绝
- [ ] 5.3 接入本班材料列表及上传成功后的刷新反馈；verify：端到端测试确认教师上传后同班列表可查到且跨班材料不可见

## 6. Compose 集成与安全验收

- [ ] 6.1 使用 Docker Compose 启动 Flask、SQLite 持久化卷和材料存储，执行迁移、预置数据和最小登录/上传/列表 smoke test；verify：容器可重复启动，`GET /health` 成功
- [ ] 6.2 执行认证、角色拒绝、学生上传 403、跨班隔离、密码哈希、密钥来源和错误响应的完整测试套件；verify：测试覆盖 spec 中的关键成功与拒绝场景
- [ ] 6.3 补充部署与安全运维文档，说明环境变量、生产密钥注入、HTTPS/cookie/CSRF、迁移和回滚步骤，并确认无真实密钥；verify：文档审查通过
- [ ] 6.4 执行 OpenSpec 严格校验；verify：运行 `openspec validate "auth-rbac-class-material-isolation" --type change --strict` 返回变更有效